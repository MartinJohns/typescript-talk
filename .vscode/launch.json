{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch current TS file",
            "runtimeArgs": [ "-r", "ts-node/register" ],
            "args": [ "${relativeFile}" ],
            "cwd": "${workspaceFolder}"
        }
    ]
}